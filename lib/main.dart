import 'package:flutter/material.dart';

void main() {
  runApp(OurInstagramClone);
}

// widget - buttons, p, headers, list, links, image


class OurInstagramClone extends StatelessWidget {
  const OurInstagramClone({super.key})
}